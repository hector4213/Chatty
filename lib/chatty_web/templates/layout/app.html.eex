<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>Chatty · Phoenix Framework</title>
    <link rel="stylesheet" href="<%= Routes.static_path(@conn, "/css/app.css") %>"/>
    <script defer type="text/javascript" src="<%= Routes.static_path(@conn, "/js/app.js") %>"></script>
  </head>
  <body>
    <header class="bg-gradient-to-r from-red-400 to-blue-500 p-4">
      <nav class="w-full p-0 flex flex-col justify-evenly md:flex-row md:justify-between  items-center >
        <div class="py-1">
          <%= link "Chatty", to: Routes.room_path(@conn, :index), class: "font-medium md:ml-4 text-xl uppercase tracking-widest"%>
        </div>
        <%= if @signed_in? do %>
        <div class="w-48 flex flex-col items-center md:w-72 md:flex-row">

          <h3 class=" p-2 text-center font-medium"> Signed in as : <%= Chatty.Accounts.current_user(@conn).username %> </h3>

          <div>
          <%= link "Sign Out", to: Routes.session_path(@conn, :delete), method: :delete, class: "px-4 py-2 rounded-full text-white bg-black" %>
          <div>
        <% else %>
        <div class="w-48">
          <%= link "Sign Up", to: Routes.registration_path(@conn, :new), class: "px-4 py-2 text-base rounded-full text-white bg-green-400" %>
          <%= link "Log in", to: Routes.session_path(@conn, :new), class: "px-4 py-2 text-base rounded-full text-white bg-green-400"%>
        </div>
      <% end %>
      </nav>
    </header>
    <%= unless is_nil(get_flash(@conn, :info)) do %>
      <p class="bg-red-400" role="alert"> <%= get_flash(@conn, :info) %></p>
    <% end %>
    <%= unless is_nil(get_flash(@conn, :error)) do %>
      <p class="bg-red-400" role="alert"> <%= get_flash(@conn, :error) %></p>
    <% end %>
    <main role="main" class="min-w-screen">
      <%= @inner_content %>
    </main>
    <script>window.userToken = "<%= assigns[:user_token] %>";</script>
  </body>
</html>
